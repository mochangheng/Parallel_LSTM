#include <eigen_matrix.hpp>

EigenMatrix operator+(double lhs, const EigenMatrix& rhs);
EigenMatrix operator-(double lhs, const EigenMatrix& rhs);
EigenMatrix operator*(double lhs, const EigenMatrix& rhs);
